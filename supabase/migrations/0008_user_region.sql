-- ============================================================
--  0008: ユーザーの放送地域設定
--  ICSフィードとTOPの「この後の放送」で、住んでいる地域で実際に
--  放送される局を代表に選ぶための地域設定。値は 'kanto'|'kansai'|'chubu'|'bs'。
--  未設定(null)は関東(kanto)として扱う。
-- ============================================================

alter table app_users add column if not exists region text;
